interface CommentProps {
	author: string
	authorId: string
	message: string
}

const Comment: React.FC<CommentProps> = ({ authorId, author, message }) => {
	return <div></div>
}

export default Comment
